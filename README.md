# joshuabae.github.io

Personal website. Pushing to `main` triggers the GitHub Actions workflow in `.github/workflows/deploy.yml`, which builds the site and deploys it to GitHub Pages automatically.

- Content lives in `src/content/experience.ts` and `src/content/posts.ts`.
- See `GITHUB-PAGES.md` for details.
